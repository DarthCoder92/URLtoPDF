<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>URL to PDF Converter</title>
        <link rel="stylesheet" href="/style.css">
    </head>
    <body>
        <form action="/" method="GET" class="home-button">
            <button type="submit">Home</button>
        </form>
      <%  if (pageState === "home") { %>
        <div class="container">
        <h1>Convert URL to PDF</h1>
        <p>Enter your URL below</p>
        <form action="/convert" method="POST">
            <input type="text" name="url" placeholder="Enter URL here" required>
            <button type="submit">Convert to PDF</button>
        </form>
        </div>
      <%  } else { %>
        <div class="container">
            <h1>Your pdf is ready!</h1>
            <a href="<%= pdfLink %>" target="_blank"><button>Download</button></a>
            <% console.log(pdfLink); %>
        </div>
      <%  } %>
    </body>
    <footer>Hanun Sreedharan © 2025</footer>
</html>